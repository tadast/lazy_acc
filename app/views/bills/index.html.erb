<h1>Listing bills</h1>

<table>
  <tr>
    <th>Bucket</th>
    <th>Title</th>
    <th>Description</th>
    <th>Amount</th>
    <th>Start date</th>
    <th>End date</th>
    <th>Periodicity</th>
  </tr>

<% @bills.each do |bill| %>
  <tr>
    <td><%=h bill.bucket_id %></td>
    <td><%=h bill.title %></td>
    <td><%=h bill.description %></td>
    <td><%=h bill.amount %></td>
    <td><%=h bill.start_date %></td>
    <td><%=h bill.end_date %></td>
    <td><%=h bill.periodicity %></td>
    <td><%= link_to 'Show', bill %></td>
    <td><%= link_to 'Edit', edit_bill_path(bill) %></td>
    <td><%= link_to 'Destroy', bill, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New bill', new_bill_path %>